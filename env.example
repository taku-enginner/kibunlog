# Cloudflare Tunnel 経由で公開するときのドメイン (Tailscale 経由のみのときは未設定でも可)
PUBLIC_DOMAIN=kibunlog.takakusagi.dev

# 既存の Docker volume (kibunrogu_db_data, kibunrogu_uploads_data) を引き継ぐため固定。
# ローカルのリポジトリディレクトリを kibunlog にリネームしても volume 名は変えない。
COMPOSE_PROJECT_NAME=kibunrogu
